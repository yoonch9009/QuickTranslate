name: Build Windows EXE

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag to create (e.g. v0.2.0). Leave blank to only build the artifact."
        required: false
        default: ""
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .
          pip install pyinstaller

      - name: Build EXE (onefile)
        run: pyinstaller --noconsole --onefile --name QuickTranslate --paths src run_quicktranslate.pyw

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: QuickTranslate-windows
          path: dist/QuickTranslate.exe

      - name: Publish Release
        if: startsWith(github.ref, 'refs/tags/') || inputs.tag != ''
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ startsWith(github.ref, 'refs/tags/') && github.ref_name || inputs.tag }}
          files: dist/QuickTranslate.exe
          generate_release_notes: true
